// Load array of project data.
const data = require('json/projects.json');

// Return array of project data as state.
const projectData = (state=data, action) => {
  return state;
}

// Export state.
export default projectData;
